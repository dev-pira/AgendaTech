# Copie este arquivo para .env (na raiz do repo) para customizar o ambiente
# do `docker compose up`. Opcional — sem ele, valores padrão de desenvolvimento
# são usados (não use os padrões em produção).

JWT_SECRET=troque-por-um-segredo-forte-antes-de-subir
